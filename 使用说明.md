# 使用说明

## 第一次使用

双击运行 `屏幕亮度调节助手 - Thebzk@52pojie.exe`。

打开后可以选择下面几种模式：

- 标准：日常办公和普通使用
- 护眼：降低亮度并增加暖色效果
- 防蓝光：更明显地降低冷色和蓝光观感
- 游戏：提高亮度和清晰度观感

调整完成后，软件会保存最后一次设置。

## 开机恢复

软件会创建一个登录恢复任务，用来在开机登录后恢复上一次设置。

这个任务不会弹出主界面。标准和游戏模式会恢复后退出；护眼和防蓝光模式为了保留滤镜效果，会隐藏运行。

## 台式机说明

台式机可以使用。外接显示器如果支持 DDC/CI，软件会尝试直接调节显示器硬件亮度。

如果显示器不支持 DDC/CI，亮度可能无法直接写入显示器硬件，但护眼、防蓝光和 Gamma 辅助效果仍可使用。

## 重新调整

如果不满意当前模式或亮度，重新打开软件调整即可。

## 注意事项

- 某些显示器需要在显示器菜单中打开 DDC/CI。
- 护眼和防蓝光模式如果关闭后效果消失，说明当前设备不稳定支持 Gamma，需要保持隐藏滤镜进程。
- 如果不想继续使用，可以在 Windows 任务计划程序中删除 `屏幕亮度调节助手` 任务。
